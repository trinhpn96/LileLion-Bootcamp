import { Router } from "express";
import User from "../models/User";

const router = Router();

//Add a user
router.post


//Get all users

router.get("/", (req, res) => {
    res.json()
});

export default router;
